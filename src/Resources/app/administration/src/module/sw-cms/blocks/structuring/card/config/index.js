import template from './sw-cms-block-config-card.html.twig';

const { Component } = Shopware;

Component.extend('sw-cms-block-config-card', 'sw-cms-block-additional-config-base-form', {
    template
});
