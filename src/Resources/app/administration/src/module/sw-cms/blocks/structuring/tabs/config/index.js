import template from './sw-cms-block-config-tabs.html.twig';

const { Component } = Shopware;

Component.extend('sw-cms-block-config-tabs', 'sw-cms-block-additional-config-base-form', {
    template
});
