import template from './sw-cms-block-config-slider.html.twig';

const { Component } = Shopware;

Component.extend('sw-cms-block-config-slider', 'sw-cms-block-additional-config-base-form', {
    template
});
