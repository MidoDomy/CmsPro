import template from './sw-cms-block-config-timeline.html.twig';

const { Component } = Shopware;

Component.extend('sw-cms-block-config-timeline', 'sw-cms-block-additional-config-base-form', {
    template
});
